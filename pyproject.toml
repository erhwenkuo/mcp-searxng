[project]
name = "mcp-searxng"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdownify>=1.1.0",
    "mcp>=1.6.0",
]
